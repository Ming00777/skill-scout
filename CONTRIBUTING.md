# 贡献指南

感谢你想让 skill-scout 更好。这个项目最需要的贡献不是代码，是**信息**。

## 最欢迎的三类贡献

### 1. 补充新的 Agent 目录（最需要）

Agent 生态变化很快，新工具每周都在出现。如果你的 Agent 不在支持列表里：

1. 先确认它的 Skill 目录真实路径，在 `~/.your-agent/skills/` 下放一个测试 Skill 验证能被加载
2. 编辑 `references/agent-registry.md`，按现有格式追加。需要给出：Agent 名称、Skill 目录绝对路径、检测依据（配置文件名 / 环境变量 / 进程名）
3. 提 PR 时附上你的验证方式，说明你实测过哪个版本

**不接受**凭文档推测的目录路径。没实测过就不要提交——推荐一个装不上的目录，比不推荐更糟。

### 2. 改进检索与评分

`references/search-playbook.md` 里定义了检索式写法和评分权重。如果你发现某类查询总是搜不准，改这里并说明你测过的样例。

### 3. 补安全规则

`references/security-checklist.md` 是安装前审计清单。发现新的风险模式（比如某种绕过方式）请补进去。

## 硬约束

**Python 必须零依赖。** 所有脚本只能用标准库，这是刻意的——有第三方依赖就要装环境，装环境就没人用。CI 会检查这一点。

其他原则（这些来自 `SKILL.md`，对整个仓库都适用）：

- **不许编造。** 仓库地址、Skill 名称、能力描述，只能是真实检索到的。搜不到就明说没找到。
- **执行远程代码前必须问。** 任何会下载并执行第三方包的命令，都要先告诉用户。
- **处境数据不出本机。** 从 `git log`、目录结构推断出的信息只在本地用来拼关键词，发往外部接口的只有关键词本身。

## 提交前

- Python 脚本过 `python -m compileall scripts/`
- Shell 脚本过 `bash -n`
- 没引入新的第三方依赖

## License

提交即表示你的贡献以 MIT 许可发布，与仓库保持一致。
